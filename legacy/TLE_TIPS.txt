定数倍ぐらいでTLEした場合の高速化

# 入出力が1e5以上ある時
-- quickio(); を呼ぶ
-- endl ではなく "\n" を使う

# データ構造
-- set ではなく map を使う
-- map ではなく unordered_map を使う（走査してかつその順番に意味がある場合を除く）
-- class を作って捨てる…を1e5回程度繰り返していないか
-- 共通部分をもつデータを作りすぎていないか
-- パラメータが小さい(20程度)ものに関するループが無駄に2倍等になっていないかを疑う

# バグ
-- 再帰や while から抜けられない